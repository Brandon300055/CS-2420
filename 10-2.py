#Inserting
def inserting():
    l = []
    for line in fin:
        #make line into studentList

        #check for CreateRandomListWithoutDuplicates
        repeats = indexOfSmallestNumber
        for i in l:
            if s2 in l:
                if s.mssn == s2.mssn:
                    repeat = True
                    print("error")
                #only add s to l if reapt flag is still false
                if repeat == False:
                    L.append(s)
                #traverse

                #delete

                #retrieve
